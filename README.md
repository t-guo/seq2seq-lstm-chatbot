# Seq-2-Seq LSTM Chatbot

```/notebook``` contains notebooks that train a LSTM chatbot using tweets and replies.

```/chatbot``` contains a flask UI that facilitates interactions with the trained bot.
